# Requirements

To run **AutoComply**, you need:

- **Python 3.6+** installed on your system.

## Dependencies
This project uses only Python's **standard library**, so no external package installation (like `pip install`) is required.

Standard libraries used:
- `json`
- `os`
